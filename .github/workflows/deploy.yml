name: Film Bot CI

#on:
#  push:
#    tags:
#      - '[0-9]+\.[0-9]+\.[0-9]+'

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build image
#        uses: actions/checkout@v3
        run: echo hello world